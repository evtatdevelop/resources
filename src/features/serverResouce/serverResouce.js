import styles from './serverResouce.module.scss';

export const ServerResouce = () => {

  return (
    <section className={styles.serverResouce} >
      <fieldset>
        <legend>Требования к серверному ресурсу</legend>
        <div>
          SERVER
        </div>
      </fieldset>

    </section>    
  )
}

